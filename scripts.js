const menuIcon = document.querySelector(".buger-menu");
const navigation = document.querySelector(".navigation");
menuIcon.addEventListener("click", function () {
  navigation.classList.toggle("change");
});
